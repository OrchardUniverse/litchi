Here is the user's query:

```
{{ query }}
```

Understand the user's query and explain with the related source files:

{% for item in file_content_list %}
{{ item.file }}:

{{ item.content }}
{% endfor %}